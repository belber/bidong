# AGENTS.md

## 这是什么

B站视频收藏助手（bili-collector）：微信小程序，把 B站视频存成卡片（标题/封面/UP主/分区），用标签分类，按收藏月份分组，点卡片跳回 B站。

只存元数据 + 转存的缩略图，不下载视频。

## 目录

- `docs/` 设计文档（spec.md 是主文档）
- `miniprogram/` 小程序前端
- `server/` FastAPI 后端

## 技术栈

微信原生小程序 · Python FastAPI · PostgreSQL · 腾讯云 COS · 腾讯云服务器

## 铁律

- 后端是纯 REST API，前端可随时重写，后端不动。
- B站接口只走后端；封面必须下载转存到 COS，不能直接外链。
- 改代码必须 TDD：先写失败的测试，再实现让它通过，最后重构。
- 设计变更先改 `docs/spec.md`，再动代码。

## 核心决策

- 「分组」不单独建表，就是打标签。
- 月份从收藏时间派生，冗余存 `YYYY-MM`，与标签正交。
- Phase 1：机器人账号监听私信/@，`@机器人 + 链接 + #标签` 自动收藏。

## 路线

1. Phase 0 核心闭环：解析链接 → 存卡片 → 标签/月份分组 → 微信登录 → 上线
2. Phase 1 机器人触发：账号监听、激活码绑定、@ 自动收藏（小规模试点）
